<div class="container">
  <div claas="row">
    <div class="col-6 offset-3">
      <h2 class="mt-5 text-center">ログイン</h2>
      <h4 class="mt-5 text-center">会員の方はこちらからログイン</h4>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <table class="mt-5 col-12">
        <tr><th><%= f.label :"メールアドレス" %></th><td class="pl-5"><%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control form-control-sm" %></td></tr>
        <tr><th><%= f.label :"パスワード" ,class:"mt-3"%></th><td class="pl-5"><%= f.password_field :password, autocomplete: "current-password", class:"form-control form-control-sm mt-3" %></td></tr>
      </table>

      <div class="actions mt-5 text-center">
        <%= f.submit "ログイン" , class: "btn btn-primary" %>
      </div>
      <% end %>
    </div>
    <div class="text-center mt-5">
      <h4>会員登録がお済みでない方</h4>
      <p><%= link_to "こちら" , new_customer_registration_path %>から新規会員登録を行ってください。</p>
    </div>
  </div>
</div>

